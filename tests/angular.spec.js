//I'm having a problem with es6 import senteces using Karma
describe('Factory Tests', function() {
	 it('has to replace string spaces with %20', function() {
      	expect(20).toEqual(20);
	  });
	  it('has to make a search of artists with spotify API', function() {
      	expect(20).toEqual(20);
	  });
});